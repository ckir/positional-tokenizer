{
  "name": "positional-tokenizer",
  "version": "1.0.6",
  "description": "Tokenizes a text using regex rules and returns the tokens with their positions in the text given.",
  "main": "dist/index.js",
  "keywords": ["tokenizer", "tokenize", "parser", "regex", "regexp", "token", "tokens"],
  "author": "Serhii Kovalenko",
  "license": "MIT",
  "type": "module",
  "bugs": {
    "url": "https://github.com/cmajsmith/positional-tokenizer/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cmajsmith/positional-tokenizer.git"
  },
  "scripts": {
    "test": "jest --maxWorkers=50% --config jestconfig.json",
    "test-watch": "jest --watch --maxWorkers=25% --config jestconfig.json",
    "build": "tsup src/index.ts --format cjs,esm --dts --clean"
  },
  "devDependencies": {
    "@swc/jest": "^0.2.26",
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4"
  },
  "files": [
    "dist"
  ]
}
